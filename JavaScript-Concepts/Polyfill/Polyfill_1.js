Object.prototype.chai= function() {
    console.log('chai')
}
const arr = [1,2,3]

if(!Array.prototype.fill) {
    // Fallback/Backup --  Polyfill
    Arrays.prototype.fill = function(){

    }
}

const str ='Piyush'

console.log(str.length)

const obj={x:1, length:1}

obj.chai()

